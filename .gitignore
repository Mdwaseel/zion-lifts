# --- python / django ---
.venv/
__pycache__/
*.py[cod]
backend/db.sqlite3
backend/staticfiles/
backend/uploads/
backend/.env

# --- node / vite ---
node_modules/
frontend/dist/
frontend/.vite/
npm-debug.log*

# --- generated media -------------------------------------------------------
# frontend/public/media is built from the masters by the scripts in
# assets-src/, but it is versioned: the static site on Vercel is deployed
# straight from the repository and needs every image and film in it.
# frontend/public/api is the API snapshot (npm run snapshot) and ships too.

# --- source masters (large; keep in the studio's own storage) --------------
videos-20260828T181837Z-1-004/

# --- editors / os ---
.DS_Store
Thumbs.db
.idea/
.vscode/
