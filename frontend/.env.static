# Loaded by `vite build --mode static` (npm run build:static).
# The site reads its content from the JSON snapshot in public/api instead of
# a running Django API, and the forms explain that they cannot send.
VITE_STATIC_API=1
