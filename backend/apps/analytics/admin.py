"""Nothing to register.

Analytics is read through the control room's own dashboard, which asks questions
of the tables rather than listing their rows. A Django admin changelist over
several million page views is a way to lock a database, not a feature.
"""
